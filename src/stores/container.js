'use strict';

import { Container } from 'flux/utils';

/* Used to subscribe a react component to multiple stores.
 * The stores that are used must be returned from
 * a static `getStores()` method.
 */