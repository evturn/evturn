@import '../../styles/all.pre';

.overlay {
  overflow: hidden;
  position: fixed;
  z-index: 99;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,.65);
  top: 0;
  left: 0;
  .transition(0.3s);
  @media(@tablet) {
    background: rgba(255,255,255,.65);
  }
}