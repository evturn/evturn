@import '../../styles/all.pre';

.logo {
  position: absolute;
  right: 0;
  left: 0;
  top: 25px;
  width: 80px;
  z-index: 3;
  margin: 0 auto;
  padding: 10px;
  border-radius: 50%;
  background-color: fade(@bg-main, 70);
  .clearfix();
  @media(@tablet) {
    padding: 20px;
    width: 140px;
  }
}

.burger {
  position: absolute;
  top: 25px;
  right: 25px;
  height: 47px;
  width: 47px;
  z-index: 3;
  cursor: pointer;
  font-size: 40px;
  text-align: center;
  @media(@tablet) {
    opacity: 0.7;
    .transition(0.3s);
    &:hover {
      opacity: 1;
    }
  }
}

.logo-about {
  composes: logo;
}

.logo-work {
  display: none;
}

.logo-contact {
  display: none;
  @media(min-width: 600px) {
    composes: logo;
    display: block;
  }
}

.logo-home {
  composes: logo;
  padding: 20px;
  width: 140px;
  @media(@tablet) {
    top: 15%;
  }
}

.burger-home,
.burger-contact {
  composes: burger;
  color: @tx-light;
}

.burger-work {
  composes: burger;
  position:fixed;
  color: @color-accent;
}

.burger-about {
  composes: burger;
  color: @tx-dark;
}

.spin {
  .rotate-clockwise(0.750s);
}